# Home-Lab
This project documents the setup of a cybersecurity home lab environment from scratch on a personal Windows device. The goal of Part 1 was to install and configure Oracle VirtualBox, build custom virtual networks via the command line, and deploy a Kali Linux virtual machine connected to those networks. 
